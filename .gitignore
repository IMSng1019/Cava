# gradle

.gradle/
build/
out/
classes/

# eclipse

*.launch

# idea

.idea/
*.iml
*.ipr
*.iws

# vscode

.settings/
.vscode/
bin/
.classpath
.project

# macos

*.DS_Store

# fabric

run/

# local research / verification artifacts (third-party sources, not part of the mod)
.research/
.probe/
.cava-research/
spike/

# tools/ SOURCES are tracked (mapquery.cjs / gen-pathfind-vectors.cjs / *.ps1 ...):
# they are the reproducible evidence behind docs/CAVA-dev-toolbox.md.
# Only the bulky downloaded artifacts are ignored.
tools/cmake/
tools/decompiler/

# AgentTeams orchestration state (never commit)
.agent-teams/

# local build/runtime caches that must stay inside the workspace (sandbox cannot write outside)
.gradle-home/
testbed/

# CMake build trees
build/native-*/
cmake-build-*/

# compiled native artifacts: produced by CMake, shipped inside the jar at build time.
# never committed -- regenerated per platform (see docs/CAVA-build.md).
natives/

# 服务端 modpack 参考 jar（174.6 MB / 49 个）—— 明确不入库（用户决定 2026-09-22）。
# 部署测试服时用脚本从该目录拷贝到 testbed/server/mods/，不要提交。
优化模组/


# java

hs_err_*.log
replay_*.log
*.hprof
*.jfr
